# Merge Docket — PR #3

## Filing

- Pull request: #3 — `feat: begin Perch Gate process isolation`
- Base: `main`
- Head: `agent/perch-gate-isolation`
- Final verified head: `655ee757390e9d90f53971685578d6a2f9433353`
- Merge commit: `9ff4295ad97b67578ef705eef2f919764f657a60`

## Review disposition

Two actionable findings were repaired before merge:

1. Plugin stdout could corrupt the worker JSON protocol. Plugin import and analysis stdout are now redirected to stderr while stdout is reserved for one protocol response.
2. Worker launch `OSError` could leave sessions permanently running. Launch failures now become `BirdExecutionError` and durable failed-session receipts.

All review threads were resolved.

## Verification

- Workflow: `verify`
- Run: #27
- Result: success
- Verified head: `655ee757390e9d90f53971685578d6a2f9433353`
- Added regression coverage: noisy plugin output and subprocess launch failure.

## Post-merge

The connected GitHub API did not expose a push-triggered run for merge commit `9ff4295ad97b67578ef705eef2f919764f657a60` at docket creation time. Status is recorded as `not independently observed`, not success.

## Known limitations

- Process isolation is not a filesystem or network sandbox.
- Portable memory limits are not yet enforced.
- Bird subprocesses execute sequentially.
- Third-party plugin quarantine is not yet implemented.

## Rollback

```bash
git revert -m 1 9ff4295ad97b67578ef705eef2f919764f657a60
```
